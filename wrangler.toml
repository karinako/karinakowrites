# Cloudflare Pages/Workers unified configuration
# Deploy via: npx wrangler deploy

name = "writer-portfolio"
compatibility_date = "2024-04-12"

[assets]
directory = "./dist"

# If you want to add Cloudflare Workers / KV / D1 later, configure them here.
