import { defineConfig } from 'astro/config';

// https://docs.astro.build/en/reference/configuration-reference/
export default defineConfig({
  base: '/',
  output: 'static',
  site: 'https://karinakowrites.com',
});
